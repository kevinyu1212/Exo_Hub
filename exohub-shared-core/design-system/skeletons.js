/**
 * Exo-Skeleton: 데이터 로딩 중 보여줄 뼈대 애니메이션 스타일
 */
export const SKELETON_STYLE = "animate-pulse bg-gray-200 rounded";

export const SkeletonBox = ({ width, height, className }) => (
  <div 
    className={\ \} 
    style={{ width: width || '100%', height: height || '20px' }} 
  />
);
